language: java
branches:
  only:
    - develop
    - "/^release-[0-999].[0-999]$/"
    - master
  install:
    - mvnw mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
  script:
    - mvnw mvn verify
